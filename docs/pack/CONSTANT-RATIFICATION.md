# Constant ratification — Build 1 S1.8

Generated by `uv run python scripts/ratify_constants.py` on 2026-08-18. **Regenerate rather than edit**: a hand-maintained table beside a tool that computes the same thing is the defect B-03 was deleted for.

`03` §3 puts fifteen-plus tunables under an S1.8 ratification gate. `05` S1.8's checkbox named two (B1-CR-94). Every row below carries a verdict, and every `NOT RATIFIABLE HERE` row names the evidence that would settle it and who can produce it.

```
`03` §3 provisional constants -- S1.8 ratification

  RATIFIED             MAP_CONF_GRAMMAR              = 0.95
                                                       labelled-set recall by archetype {'web': 1.0, 'ai': 1.0, 'platform': 1.0, 'lowcode': 1.0, 'data': 1.0}; ladder ordering holds; `MAP_CONF_REGEX` (0.45) stays above `MAP_MIN_EMIT_CONFIDENCE` (0.4), so a regex fact is emitted rather than becoming a gap

  RATIFIED             MAP_CONF_REFLECTION           = 0.92
                                                       labelled-set recall by archetype {'web': 1.0, 'ai': 1.0, 'platform': 1.0, 'lowcode': 1.0, 'data': 1.0}; ladder ordering holds; `MAP_CONF_REGEX` (0.45) stays above `MAP_MIN_EMIT_CONFIDENCE` (0.4), so a regex fact is emitted rather than becoming a gap

  RATIFIED             MAP_CONF_DECLARED             = 0.8
                                                       labelled-set recall by archetype {'web': 1.0, 'ai': 1.0, 'platform': 1.0, 'lowcode': 1.0, 'data': 1.0}; ladder ordering holds; `MAP_CONF_REGEX` (0.45) stays above `MAP_MIN_EMIT_CONFIDENCE` (0.4), so a regex fact is emitted rather than becoming a gap

  RATIFIED             MAP_CONF_CTAGS                = 0.7
                                                       labelled-set recall by archetype {'web': 1.0, 'ai': 1.0, 'platform': 1.0, 'lowcode': 1.0, 'data': 1.0}; ladder ordering holds; `MAP_CONF_REGEX` (0.45) stays above `MAP_MIN_EMIT_CONFIDENCE` (0.4), so a regex fact is emitted rather than becoming a gap

  RATIFIED             MAP_CONF_REGEX                = 0.45
                                                       labelled-set recall by archetype {'web': 1.0, 'ai': 1.0, 'platform': 1.0, 'lowcode': 1.0, 'data': 1.0}; ladder ordering holds; `MAP_CONF_REGEX` (0.45) stays above `MAP_MIN_EMIT_CONFIDENCE` (0.4), so a regex fact is emitted rather than becoming a gap

  NOT RATIFIABLE HERE  MAP_CONF_AGENT_REVIEWED       = 0.9
                                                       no agent-authored extractor has ever been reviewed
                                                       BLOCKED ON: an adapter credential -- `05` S1.7's three open items

  RATIFIED             MAP_MIN_EMIT_CONFIDENCE       = 0.4
                                                       sits below every band, so no rung is silently unemittable; a floor above `MAP_CONF_REGEX` would turn every regex fact into a gap and the ladder's last rung into decoration

  RATIFIED             MAP_PLUGIN_COVERAGE_FLOOR     = 0.6
                                                       M2 by archetype {'web': 0.7909604519774012, 'ai': 0.6190476190476191, 'platform': 1.0, 'lowcode': 1.0, 'data': 0.0}; non-model share {'web': 0.9915254237288136, 'ai': 1.0, 'platform': 1.0, 'lowcode': 1.0, 'data': 1.0}; below the floor on ['data'] -- ruled in OD-19 (B1-CR-90): the floor's value is kept and the trigger is read against the non-model share, because `declared` evidence reaches no model. The value does not move; the reading is what was decided

  NOT RATIFIABLE HERE  MAP_GLUE_REWRITE_ALERT        = 0.4
                                                       M5 is undefined -- no agent-authored module has ever been reviewed
                                                       BLOCKED ON: an adapter credential -- `05` S1.7's three open items

  NOT RATIFIABLE HERE  MAP_STAGE1_REQUIRED_FAMILIES  = endpoint,db_field,job,config_key,prompt
                                                       `05` S1.8 ratifies this against what the cold-FDE exercise showed people reach for first, and that exercise is a timed human sitting with an unfamiliar repository
                                                       BLOCKED ON: the cold-FDE exercise -- `docs/COLD-FDE-EXERCISE.md`, which no agent can perform

  RATIFIED             MAP_MAX_FILE_BYTES            = 2000000
                                                       the soak indexed 4552 files in its largest repository with zero large-file skips reported; no real file in the corpus was lost to this bound

  RATIFIED             MAP_MAX_TREE_FILES            = 250000
                                                       the largest corpus repository indexed 4552 files, well under the sampling trigger, so no run was sampled and no map understated itself

  RATIFIED             MAP_MAX_RSS_BYTES             = 2147483648
                                                       measured on the reference corpus: peak saleor 300 MiB, langchain 145 MiB, jaffle-shop-classic 56 MiB against a 2048 MiB ceiling

  RATIFIED             MAP_OUTSIDE_VCS_RECALL_FLOOR  = 0.9
                                                       S1.5 measured outside-VCS recall 1.000 (8 of 8) on `langgraph-support` and S1.8 re-ran every labelled set at recall 1.000; the floor holds with margin and is not the binding constraint on any pack

  RATIFIED             MAP_XML_MAX_DEPTH             = 64
                                                       the two bundle archetypes (`platform`, `lowcode`) score recall 1.000 with this depth in force, so no component in either real export was lost to it -- the reversal trigger `03` §3 states

  NOT RATIFIABLE HERE  MAP_AGENT_SANDBOX_TIMEOUT_S   = 60
                                                       `03` §3 states the reversal trigger as *the first authored module that passes the static audit, is correct, and is killed by one of them*. No module has ever been authored
                                                       BLOCKED ON: `04` §8's E2, which needs an adapter credential -- the sandbox has never run an authored module

  NOT RATIFIABLE HERE  MAP_AGENT_SANDBOX_MAX_BYTES   = 536870912
                                                       `03` §3 states the reversal trigger as *the first authored module that passes the static audit, is correct, and is killed by one of them*. No module has ever been authored
                                                       BLOCKED ON: `04` §8's E2, which needs an adapter credential -- the sandbox has never run an authored module

  RATIFIED             MAP_BINARY_SNIFF_BYTES        = 8192
                                                       the soak indexed three real repositories with zero binary misclassifications visible in their fact counts or degradations

  NOT RATIFIABLE HERE  MAP_TOOL_TIMEOUT_S            = 30
                                                       the only allowlisted tool is `ctags`, which is absent on the machines this build has run on -- every ladder walk degraded past that rung rather than timing out on it
                                                       BLOCKED ON: a machine with `universal-ctags` installed, running the soak corpus

  RATIFIED             MAP_CONFIG_VALUE_MAX_CHARS    = 64
                                                       `web` recall 1.000 with this truncation in force: no labelled config identity was lost or mismatched because its default was cut

  NOT RATIFIABLE HERE  MAP_FIRST_SCREEN_LIST_MAX     = 20
                                                       this is a readability bound -- how many entries before a list stops being read -- and the only evidence that settles it is a human reading a first screen under time pressure
                                                       BLOCKED ON: the cold-FDE exercise -- `docs/COLD-FDE-EXERCISE.md`

  21 rows: 7 NOT RATIFIABLE HERE, 14 RATIFIED

  No value was changed by this tool. A revision is a clarification-register
  row with a decision and a date (`00` §9 rule 6), never a silent edit.
```
