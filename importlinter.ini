; Import contracts -- implementation spec §5.1, all twelve.
;
; These are load-bearing, not stylistic. Eight are import-graph rules and are
; declared natively below. Four are statements about source text rather than
; about imports, and are implemented as custom contract types under
; `tools/contracts/` so that all twelve live in one file and run under one
; command. A rule enforced by a second tool is a rule that eventually runs in a
; job nobody watches.
;
; Every contract is live from S0 -- before the code it constrains exists. A gate
; switched on after the fact starts life with exemptions.

[importlinter]
root_packages =
    adopt_const
    adopt_obs
    adopt_schema
    adopt_model
    adopt_scope
    adopt_identity
    adopt_store
    adopt_coverage
    adopt_freshness
    adopt_export
    adopt_detect
    adopt_policy
    adopt_agent
    adopt_workflow
    adopt_cli
include_external_packages = True
contract_types =
    no_foreign_tables: tools.contracts.NoForeignTablesContract
    no_revision_update: tools.contracts.NoRevisionUpdateContract
    no_covered_cache_write: tools.contracts.NoCoveredCacheWriteContract
    workflow_body_purity: tools.contracts.WorkflowBodyPurityContract

; --------------------------------------------------------------------------
; 1. no-raw-sqlite
; --------------------------------------------------------------------------
[importlinter:contract:no-raw-sqlite]
name = no-raw-sqlite -- only adopt_store.sqlite imports sqlite3
type = forbidden
source_modules =
    adopt_const
    adopt_obs
    adopt_schema
    adopt_model
    adopt_scope
    adopt_identity
    adopt_coverage
    adopt_freshness
    adopt_export
    adopt_detect
    adopt_policy
    adopt_agent
    adopt_workflow
    adopt_cli
    adopt_store.facades
    adopt_store.vector
forbidden_modules =
    sqlite3
; The contract follows INDIRECT chains, which is what catches `adopt_store.facades`
; or `adopt_coverage` reaching the driver through a helper -- the check those
; packages' dialect-freedom actually rests on. Applied to `adopt_cli` unchanged it
; says something stronger than implementation spec §5.1 does ("only
; `adopt_store.sqlite` imports sqlite3"): it forbids the CLI from depending on
; `adopt_store` at all, and the CLI is the composition root -- `adopt store
; doctor` and `adopt init` (`02` §14, `05` S6) exist precisely to open a store.
;
; So the wiring module, and only the wiring module, is exempted by name. It holds
; no SQL and no dialect knowledge; it resolves a path and calls `open_store`.
; Every other `adopt_cli` module stays fully constrained, including indirectly --
; a command reaching for the driver itself still breaks this contract.
ignore_imports =
    adopt_cli.store_option -> adopt_store
    adopt_cli.store_option -> adopt_store.api

; --------------------------------------------------------------------------
; 2. no-provider-sdk -- only adopt_agent.adapters imports a provider client
; --------------------------------------------------------------------------
[importlinter:contract:no-provider-sdk]
name = no-provider-sdk -- provider SDKs live only in adopt_agent.adapters
type = forbidden
source_modules =
    adopt_const
    adopt_obs
    adopt_schema
    adopt_model
    adopt_scope
    adopt_identity
    adopt_store
    adopt_coverage
    adopt_freshness
    adopt_export
    adopt_detect
    adopt_policy
    adopt_workflow
    adopt_cli
forbidden_modules =
    anthropic
    openai
    litellm
    cohere
    mistralai
    vertexai
; Google's SDK ships as the namespace subpackage `google.generativeai`, and
; import-linter cannot express a subpackage of an external package as a
; forbidden module. It is covered instead by the licence gate: no provider SDK
; can enter the tree without a `licence-verifications.md` row, and adding one
; is the moment its top-level module is added to this list.

; --------------------------------------------------------------------------
; 3. no-dbos -- no DBOS symbol anywhere in the open repository
; --------------------------------------------------------------------------
[importlinter:contract:no-dbos]
name = no-dbos -- DBOS is imported only by plane_workflow.dbos_backend
type = forbidden
source_modules =
    adopt_const
    adopt_obs
    adopt_schema
    adopt_model
    adopt_scope
    adopt_identity
    adopt_store
    adopt_coverage
    adopt_freshness
    adopt_export
    adopt_detect
    adopt_policy
    adopt_agent
    adopt_workflow
    adopt_cli
forbidden_modules =
    dbos

; --------------------------------------------------------------------------
; 4. no-vector-impl -- only adopt_store.vector imports a vector driver
; --------------------------------------------------------------------------
[importlinter:contract:no-vector-impl]
name = no-vector-impl -- vector drivers live only in adopt_store.vector
type = forbidden
source_modules =
    adopt_const
    adopt_obs
    adopt_schema
    adopt_model
    adopt_scope
    adopt_identity
    adopt_coverage
    adopt_freshness
    adopt_export
    adopt_detect
    adopt_policy
    adopt_agent
    adopt_workflow
    adopt_cli
    adopt_store.facades
    adopt_store.sqlite
forbidden_modules =
    sqlite_vec
    faiss
    chromadb
    pgvector

; --------------------------------------------------------------------------
; 5. no-raw-psycopg -- declared here, enforced in adopt-plane
;    The open repository must not import psycopg at all: the Postgres
;    realization lives entirely in the closed repository.
; --------------------------------------------------------------------------
[importlinter:contract:no-raw-psycopg]
name = no-raw-psycopg -- psycopg appears only in plane_store.postgres
type = forbidden
source_modules =
    adopt_const
    adopt_obs
    adopt_schema
    adopt_model
    adopt_scope
    adopt_identity
    adopt_store
    adopt_coverage
    adopt_freshness
    adopt_export
    adopt_detect
    adopt_policy
    adopt_agent
    adopt_workflow
    adopt_cli
forbidden_modules =
    psycopg
    psycopg2
    asyncpg

; --------------------------------------------------------------------------
; 6. core-independent -- the open repository never imports the closed one
;    This is the licence boundary expressed as an import rule.
; --------------------------------------------------------------------------
[importlinter:contract:core-independent]
name = core-independent -- no adopt_* package imports any plane_* package
type = forbidden
source_modules =
    adopt_const
    adopt_obs
    adopt_schema
    adopt_model
    adopt_scope
    adopt_identity
    adopt_store
    adopt_coverage
    adopt_freshness
    adopt_export
    adopt_detect
    adopt_policy
    adopt_agent
    adopt_workflow
    adopt_cli
forbidden_modules =
    plane_const
    plane_store
    plane_workflow
    plane_api

; --------------------------------------------------------------------------
; 7. const-leaf -- adopt_const imports nothing first-party
; --------------------------------------------------------------------------
[importlinter:contract:const-leaf]
name = const-leaf -- adopt_const is a leaf
type = forbidden
source_modules =
    adopt_const
forbidden_modules =
    adopt_obs
    adopt_schema
    adopt_model
    adopt_scope
    adopt_identity
    adopt_store
    adopt_coverage
    adopt_freshness
    adopt_export
    adopt_detect
    adopt_policy
    adopt_agent
    adopt_workflow
    adopt_cli

; --------------------------------------------------------------------------
; 8. generated-purity -- adopt_model imports only pydantic and adopt_const
; --------------------------------------------------------------------------
[importlinter:contract:generated-purity]
name = generated-purity -- adopt_model imports only pydantic and adopt_const
type = forbidden
source_modules =
    adopt_model
forbidden_modules =
    adopt_obs
    adopt_schema
    adopt_scope
    adopt_identity
    adopt_store
    adopt_coverage
    adopt_freshness
    adopt_export
    adopt_detect
    adopt_policy
    adopt_agent
    adopt_workflow
    adopt_cli
    sqlite3
    psycopg

; --------------------------------------------------------------------------
; 9. no-foreign-tables (custom) -- CREATE TABLE only in generated migrations
;    and in the emitters that generate them from the manifest (CR-24). The rule
;    forbids a table the canonical manifest does not know about; the emitter is
;    the code that reads the manifest, so exempting it narrows nothing.
; --------------------------------------------------------------------------
[importlinter:contract:no-foreign-tables]
name = no-foreign-tables -- CREATE TABLE exists only in schema/migrations and the manifest emitters
type = no_foreign_tables
paths =
    packages
    scripts
    tools
    bench
    schema
allowed_paths =
    schema/migrations
    packages/adopt-schema/src/adopt_schema/emitters

; --------------------------------------------------------------------------
; 10. no-revision-update (custom) -- the append-only guarantee
; --------------------------------------------------------------------------
[importlinter:contract:no-revision-update]
name = no-revision-update -- no UPDATE targets a *_revision table
type = no_revision_update
paths =
    packages
    scripts
    tools
    bench
    schema
allowed_paths =
    tests/fixtures/planted

; --------------------------------------------------------------------------
; 11. no-covered-cache-write (custom) -- only adopt_coverage writes the cache
; --------------------------------------------------------------------------
[importlinter:contract:no-covered-cache-write]
name = no-covered-cache-write -- only adopt_coverage writes covered_cache
type = no_covered_cache_write
paths =
    packages
    scripts
    tools
    bench
    schema
allowed_paths =
    packages/adopt-coverage
    schema/migrations
    tests/fixtures/planted

; --------------------------------------------------------------------------
; 12. workflow-body-purity (custom) -- a replayed body is deterministic
; --------------------------------------------------------------------------
[importlinter:contract:workflow-body-purity]
name = workflow-body-purity -- no clock, randomness, network, model call or I/O in a @workflow body
type = workflow_body_purity
paths =
    packages
    scripts
    tools
    bench
