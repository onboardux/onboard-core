[project]
name = "adopt-detect"
version = "0.3.0.dev0"
description = "Archetype detection, tier negotiation, the observability boundary. Implemented in S6."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/adopt_detect"]
