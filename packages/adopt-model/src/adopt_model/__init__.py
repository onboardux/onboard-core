"""GENERATED typed models, one per table, enum and wire shape.

Empty by design at S0. Generated in S1 by ``adopt-schema generate``.

**Never hand-edited.** CI regenerates this package and compares; a hand edit is
`SCHEMA_GENERATED_DRIFT`. This package may import only ``pydantic`` and
``adopt_const`` -- enforced by the `generated-purity` import contract.
"""
