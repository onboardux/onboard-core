[project]
name = "adopt-schema"
version = "0.3.0.dev0"
description = "Manifest loader, four emitters, the additive-only linter, and migrations. Implemented in S1."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/adopt_schema"]
