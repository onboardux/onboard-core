[project]
name = "adopt-scope"
version = "0.3.0.dev0"
description = "The firm-engagement-system-environment hierarchy, slugs, lifecycle events. Implemented in S2."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/adopt_scope"]
