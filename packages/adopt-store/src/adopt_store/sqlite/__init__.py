"""The only module in the programme permitted to import `sqlite3`.

Empty by design at S0. Implemented in S2.

This module exists now so the `no-raw-sqlite` import contract has a concrete
target from the first commit rather than from the sprint that fills it in.
"""
