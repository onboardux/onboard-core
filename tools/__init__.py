"""Build-time tooling for `adopt-core`.

Not shipped: nothing under `tools/` is packaged into a wheel or a binary. It
exists to enforce the rules in the implementation spec that a type checker and
an import graph cannot express on their own.
"""
